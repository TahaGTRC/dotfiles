. "$SCRIPTS/locations.sh"
doas mkdir -p "$XDG_RUNTIME_DIR"
doas rm -rf "$XDG_RUNTIME_DIR/*"
doas chown taha:taha "$XDG_RUNTIME_DIR"
doas chmod 700 "$XDG_RUNTIME_DIR"
pipewire && \
pipewire-pulse && \
wireplumber
